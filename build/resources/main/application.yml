server:
  port: 8080
spring:
  jpa:
    generate-ddl: false
    show-sql: true
    hibernate:
      ddl-auto: create
  h2:
    console:
      enabled: true

user:
  ribbon:
    listOfServers: localhost:9090
demo:
  ribbon:
    listOfServers: localhost:9091